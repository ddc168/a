import { P3 } from '../lib/db';
// const pg = require('pg')
const fs = require("fs")
const os = require("os")
const path = require("path")
const readline = require('line-read')
    
export function p3(){
    // P3.find({},{sort: {$seq:1}}).forEach(e => {
    //     fs.appendFileSync("c:/dockerShare/p3.txt", e.seq + e.link + e.name + 
    //     e.author + e.source + e.date + e.data + os.EOL)
    // });
    // + e.quote + e.download


    // f = fs.readFileSync("c:/dockerShare/p3.txt", 'utf-8')
    
    // readline.readLineFromFile("c:/dockerShare/p3.txt").join(function(xs) {
    //     fs.writeFileSync("c:/dockerShare/p3.csv", xs)
    // });
}

